# ImageViewer v2.2.0 发布前审查

日期：2026-09-08。首个公开版本，Windows x64 便携包。

## 汇总

本地 Debug、Release 各 50 项回归通过；Debug 在 150% 缩放下重复通过；CMake Release 的 CTest 1/1 通过。缩略图改动符合本轮明确要求。远端构建、正式包启动及资产校验已闭环，v2.2.0 已公开发布，证据见末节。未签名及尚未完整覆盖的环境限制如实保留。

## 仓库与代码审查范围

主仓库 `ImageViewer`，无子模块、无历史发布标签。本次统一以修改前远端 main `70a10cf080c9cfc1673e28132302354b296f18fa` 为对比基准，审查至 `ad630a8` 及本轮待提交的缩略图改动。没有将后续文档本身计入代码变更规模。

> 变更规模：基准至当前已跟踪工作区共 70 个文件，新增 4346 行、删除 392 行（生成本报告之前查询）。

逐项查阅关键提交的实际 diff：`dd8e433` 的异步加载与安全保存；`1ea2c94`、`16705ee` 的窗口、状态栏和侧边按钮；`da75c98`、`f9ee59e` 的通道统计、导航焦点及开源配置；`83888de` 的直方图平滑；`4220f21` 的分析取消、对比状态、参数输入与单实例转发；`ad630a8` 的部署库补齐。另检查本轮删除搜索映射后的加载索引、滚轮处理、角标绘制和对应回归。历史 OpenCV 导入库的内部行为未作源码验证；公开 CI 从固定官方源码构建。

### 功能完善

- 彩色直方图叠加显示，支持曲线平滑、原始计数与手动 ROI 分析。
- 状态栏像素分色、大字体、圆角、图标与两侧翻图。
- 分析对象选择、单参数输入和复原、文件转发、结果原子保存。
- 增加源码许可、独立构建、便携包、校验值和构建元数据。

### 修复现象

- 首尾连续按方向键不再拖动图像，参数输入不再被翻图抢占。
- 快速切图不会回显已过期图像或统计；隐藏分析会取消任务。
- 修改处理参数后保留分割对比状态；灰度像素不显示负色相。
- 便携包补齐并发和本地通信运行库，减少启动时缺 DLL。

### 明确取消或变更

本轮用户明确要求取消搜索、缩略图滚动条和大小选择。实现为固定 80×54 缩略图、每项左上角“序号/总数”；移除整行搜索/定位/大小工具。保留 Ctrl+T、滚轮横向浏览及当前项自动进入视口。原文档中 S09 的历史回复保留，以本轮要求为准。

## 影响模块与复测建议

### 高优先级：加载、导航、保存

- 代码影响：加载代次检查连接到原有目录模型、预加载和缓存；`QSaveFile` 的提交结果决定是否显示保存成功。
- 场景：连续翻图后立即换目录；首尾重复按键；打开损坏图片；中文路径另存处理结果。
- 标准：主图与当前序号同步；坏图保留上一个成功索引；不会用过期结果覆盖新图；导出像素与处理结果一致，失败不破坏原文件。
- 证据：本地两种配置的异步、坏图、保存、方向键回归均通过。

### 高优先级：手动分析、对比和统计

- 代码影响：分析请求捕获来源图像和取消令牌；换图、来源切换及隐藏面板会作废结果。曲线平滑使用统计副本，原始直方图保持不变。
- 场景：彩色/灰度整图及 ROI；统计期间换图或隐藏；修改参数后再次分析；分割线两侧取样。
- 标准：彩色三通道同轴，灰度单通道；悬停计数及 ROI 均值不被平滑修改；隐藏后不发布迟到结果；对比比例保留。
- 证据：RGB、平滑、取消与来源选择回归通过。

### 高优先级：启动与部署

- 代码影响：用户会话互斥锁和本地管道连接原有打开文件路径；部署脚本显式复制 Qt Network/Concurrent、CRT、图片插件和翻译。
- 场景：第二进程打开中文空格和多个文件；便携目录独立启动；PNG/JPEG/BMP/TIFF 解码；核对资产 SHA-256 与构建 revision。
- 标准：仅保留一个窗口且文件收到确认；多文件顺序正确；启动无缺库；下载包与校验文件一致且对应发布标签。
- 证据：真实子进程转发回归通过。远端便携包须在发布前下载后再验证，不以本地开发 DLL 替代。

### 中优先级：缩略图和桌面显示

- 代码影响：取消过滤后的索引直接对应目录列表；隐藏滚动条仍保留滚动模型，加载器继续预取可见区附近项目。
- 场景：30 项目录滚轮浏览、选末项、Ctrl+T；小窗口及 150% 缩放；重新启动读取旧设置。
- 标准：无搜索与尺寸选择；固定小图；角标正确；末项可见；隐藏时仍可翻图，不重复解码整个目录。
- 证据：TEST-45、TEST-50 通过，合成图界面截图已检查。完整多显示器和 200% 桌面组合留待补测。

## 发布红线

### 必须通过才能发版

- Debug/Release 构建与 50 项回归全部通过，代码编码和 diff 空白检查无错误。
- 远端独立构建成功；发布包实际启动、解码及下载完整性核对通过。
- 标签、软件版本、构建 revision 一致；公开包不得混入 Debug DLL、私人样图、凭据或开发机专用依赖。
- 每份发布包带自有及第三方许可、对应依赖来源和构建信息。

### 可以带条件发版

- 未签名便携 ZIP：发布页说明无安装程序和自动更新。
- 本地兼容 Qt 5.14.2，公开构建使用 Qt 5.15.2 / OpenCV 4.5.5；旧依赖未完成全面安全审计，保留升级事项。
- 未完成独立干净虚拟机、全量多显示器和超大图长时间压力测试，不宣称这些场景已验收。

### 不影响发版

按用户 P9 暂缓的通道隐藏/对数轴、标题信息重排、统计导出继续留在产品回复文档，不扩大首版范围。

## 整体复测与风险

本地日志保留于忽略目录 `obj/260908-first-release-*.log`。自动 UI 使用 offscreen，截图为自建合成图。复测要同时检查预处理面板、状态栏和旧快捷键，因为缩略图显隐会改变焦点和主图可用区域。大图解码仍有原图内存峰值；动画及多页格式只显示首帧/页，16 位精度分析不在本版范围内。

## 发版闭环（2026-09-08 17:59）

v2.2.0 已公开发布，对应提交 `c62963714ab5f092bf98131247135fd52a2ececb`。GitHub Actions 运行 `34211702204` 的 OpenCV 编译、工程构建、Debug/Release 回归及打包全部成功。发布过程中从实际日志定位并修复了工作流未引用逗号参数以及 OpenCV 精简构建残留 highgui 配置问题，修复提交为 `fcb2829`、`c629637`。

下载的 CI 归档及内层 ZIP 均经 SHA-256 核对，构建元数据匹配标签且工作树无修改。正式 ZIP 为 20,151,220 字节，上传后的 GitHub digest 与本地相同。匿名 API 确认 latest 为公开 v2.2.0，两个下载资产均可见。

随后从公开 Release 直链重新下载完整 ZIP，SHA-256 复核一致，下载交付已闭环。

已将 PATH 限制为 Windows 系统目录并清除 Qt/OpenCV 开发环境变量，在独立解压目录真实启动，依次打开 BMP、JPEG、PNG、TIFF。主图、尺寸、缩略图序号均正常；进程模块路径确认运行库来自便携目录。必须通过项已完成，可以按前述未签名及未完整覆盖环境的条件发版。详细下载地址与校验值见 `docs/260908首版发布记录.md`。
