# ImageViewer 产品待回复事项

日期：2026-09-07。按你的要求直接执行本轮优化，有疑问的产品决策集中写在本地。你可直接在每项“你的回复”下填写；本轮工作无需等待回复。

## Q1：隐藏预处理面板是否继续保留处理结果？

**本轮已采用：** 保留处理开关、参数和结果，分析面板可分析处理图。两个面板互斥显示。只有取消“启用预处理”或重置才恢复原图。

这替代了 V2 旧验收条款中“隐藏即恢复原图”的规则，因为旧行为阻断处理结果分析。

**你的回复：**

## Q2：换图是否保留缩放和平移？

**本轮已采用：** 成功打开新文件时适配窗口，避免同尺寸新图继承局部放大造成误判；同一文件调整算法参数保留视图。是否需要“连续比较时保持缩放”设置？

**你的回复：**

## Q3：遇到损坏图片是否自动跳过？

**本轮已采用：** 保留原图、当前索引，状态栏提示失败，不连续弹窗；用户可通过其他缩略图选择下一张。尚未自动跳过损坏文件，避免隐藏异常数据。

**你的回复：**

## Q4：ROI 在分割对比时统计哪张图？

**本轮沿用：** 像素悬停按所在左右区域读取原图/处理图；ROI 统计统一读取完整处理结果，不统计屏幕拼接图。是否需要增加“原图 / 处理图”统计源选项？

**你的回复：**

## Q5：高位深、动图和多页图的支持范围？

**当前限制：** GIF/多页 TIFF 没有播放和翻页控件；OpenCV 后备转换仅支持 8-bit；处理和统计为当前显示/转换后的数据。工业原始 16-bit 数据是否需要原值取样、窗宽窗位和多页浏览？请提供优先级与典型尺寸/文件量。

**你的回复：**

## Q6：大目录与超大图的目标规模？

**本轮已采用：** 前台解码异步且请求合并；没有引入编码器瓦片读取。目录扫描、QPixmap 更新仍可能占用 UI，邻图预加载会额外保留图像。请明确常见图像大小、单目录数量、内存上限与期望翻图延迟。

**你的回复：**

## Q7：单实例与第二次双击图片的行为？

**现存待办：** 旧 Application 单实例逻辑会退出第二个进程，未把新路径转发给已有窗口。后续应选择“复用同一窗口并转发图片”还是“允许多个窗口并排比较”。本轮未在未定窗口策略下引入进程间通信。

**你的回复：**

## Q8：发布版本与真实桌面验收？

**本轮已采用：** 保持版本号 2.1.0，完成本地中文 Git 提交。构建产物和日志留在本机，不推送远端或制作安装包。

资源管理器 → 主图区跨窗口 PNG/JPEG/BMP 拖放、调试终端开关复启、高倍率像素网格、多显示器布局、保存后缀/覆盖矩阵仍需统一人工复测。自动 Qt 拖放回归通过不能替代资源管理器真实拖放。

**你的回复：**

## Q9：字体与翻图箭头的长期偏好（2026-09-08）

**本轮已实现：** 全局 16px、标题 15px；有图时两侧箭头常驻显示，首尾禁用相应方向。暂无阻塞问题。如果后续需要可选字号（标准 / 大 / 特大）或鼠标靠近时才显示箭头，可在此统一回复；当前版本按以上默认值交付。

**你的回复：**
